# 乘客端登录系统优化第二版

## 1. 背景信息
### 1.1 v1版本存在的问题
- 硬编码敏感信息(AppID,Secret,JWT密钥)
- Token无防篡改机制(UUID可被篡改)
- 依赖Redis验证增加系统负载

### 1.2 改进目标
- 提升系统安全性
- 实现真正的无状态认证
- 标准化Token管理

## 2. 技术选型: JWT
### 2.1 为什么选择JWT?
| 特性	UUID | Token（现在的） |	JWT Token（优化的）|
|------------|----------------|-------------------|
|安全性 |	❌ 无签名，可篡改 |	✅ 有签名，防篡改 |
|信息携带 |	❌ 只是一个随机字符串 |	✅ 可携带用户信息（payload） |
|验证方式 |	❌ 必须查Redis |	✅ 本地即可验证（减少Redis查询） |
|无状态性 |	❌ 依赖Redis存储 |	✅ 自包含，真正无状态 |
|标准化 |	❌ 自定义实现 |	✅ 行业标准（RFC 7519） |
(只存储少量的,必要的,不敏感的信息)

### 2.2 JWT工作流程
1. 用户登录 → 生成JWT（不存用户信息到Redis）
2. 每次请求 → 验证JWT签名和过期时间
3. 退出登录 → JWT加入Redis黑名单（直到自然过期）
4. 获取用户信息 → JWT解析ID → 数据库

## 3. 具体实现方案
### 3.1 配置文件优化
- 把 application.properties 改成 application.yml
- 把 CustomerController 文件里 APPID 和 SECRET 移到配置文件
- JWT的secret也移到配置文件,自己选择的密钥,至少要256位（32字节）
- 配置文件又从系统环境变量中读取
- 修改过系统环境变量后要重启IDEA才会生效

### 3.2 核心组件说明
- JwtProperties - 配置属性类
- JwtTokenProvider - JWT生成/验证工具类
- TokenService - 业务层Token管理

### 3.3 安全增强
- JWT密钥长度至少256位（32字节）
- 敏感信息通过环境变量注入
- Token自动刷新机制
- Redis黑名单管理

## 4. 注意事项
- JWT一旦签发，在过期前一直有效
- 需要Redis管理黑名单实现主动退出
- Payload不宜过大（影响网络传输）

## 5. 测试验证
### 5.1 功能测试
- **JwtTokenProviderTest**
- 微信登录生成JWT
- JWT验证拦截器
- Token刷新机制
- 退出登录黑名单

**生成的JWT Token:**
eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJvOXh1XzY5X1BiVWtaaFNVdDN0QWtOWGZ0MHZvIiwiaWF0IjoxNzY3Njg4MDY5LCJleHAiOjE3Njc3NzQ0Njl9.ZiHB0eo7OfvtFxG0wEAjfXJ5aXTnbUkgIw06PojHxM4

**解码后:**
**Header:** 表示这个JWT使用HMAC SHA-256算法进行签名验证，这是一种对称加密算法，意味着签名和验证使用相同的密钥。
```json
{
"alg": "HS256"
}
```
**Payload:**
- sub：Subject，主体标识，通常是用户ID。这个值 o9xu_69_PbUkZhSUt3tAkNXft0vo 很可能是某个用户的唯一标识符。
- iat：Issued At，签发时间。时间戳 1767688069 对应的是： 
  - UTC时间：2025-12-31 23:21:09 
  - 北京时间：2026-01-01 07:21:09
- exp：Expiration Time，过期时间。时间戳 1767774469 对应的是： 
  - UTC时间：2026-01-01 23:21:09 
  - 北京时间：2026-01-02 07:21:09
```json
{
"sub": "o9xu_69_PbUkZhSUt3tAkNXft0vo",
"iat": 1767688069,
"exp": 1767774469
}
```

### 5.2 安全性测试
- **TokenServiceTest**
- JWT签名防篡改
- 过期Token自动拒绝
- 黑名单Token无法使用