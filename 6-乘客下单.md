# **乘客下单**

1. 业务概述
乘客下单是整个代驾流程的起点，用户在前端完成选点、预估价格后，点击“呼叫代驾”即进入下单环节。

2. 核心流程：
- 接收乘客下单参数（起终点、经纬度、好处费等）
- 重新计算预估里程和费用
- 保存订单信息
- 启动任务调度，搜索附近司机
- 轮询订单状态，等待司机接单

3. 分层架构
```text
乘客端Web接口 → Service → FeignClient → 订单微服务 → Mapper/DB
                                    ↘ 地图微服务
                                    ↘ 费用微服务
                                    ↘ 调度微服务(XXL-JOB)
```




---
## **功能一:乘客下单**
1. 业务逻辑
```text
乘客端 → 提交订单 → 重新计算线路 → 重新计算费用 → 保存订单 → 启动任务调度 → 返回订单ID
```

2. 流程
```text
乘客端 → 订单Controller → 订单Service → 地图Feign → 地图服务
                                  ↓
                            费用Feign → 费用服务
                                  ↓
                            订单Feign → 订单服务(保存)
                                  ↓
                            调度Feign → 调度服务(启动任务)
                                  ↓
                            返回订单ID
```

3. web-customer(OrderController)
```java
    @Operation(summary = "乘客下单")
    @PostMapping("/submitOrder")
    public Result<Long> submitOrder(@RequestBody SubmitOrderForm submitOrderForm) {
        Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
        Long customerId = Long.valueOf(authentication.getName());
        submitOrderForm.setCustomerId(customerId);
        return Result.ok(orderService.submitOrder(submitOrderForm));
    }
```
## **功能二:乘客查询订单状态**
1. 业务逻辑
乘客下单后，前端需要实时知道订单状态变化（如是否有司机接单）
订单状态为1，乘客端小程序会轮询订单状态
当订单状态为2时，说明已经有司机接单了，那么页面进行跳转，进行下一步操作


2. 流程
```text
乘客端(轮询) → 查询订单状态 → 返回状态值 → 根据状态跳转页面
```

3. web-customer(OrderController)
```java
    @Operation(summary = "查询订单状态")
    @GetMapping("/getOrderStatus/{orderId}")
    public Result<Integer> getOrderStatus(@PathVariable Long orderId) {
        return Result.ok(orderService.getOrderStatus(orderId));
    }
```
## **功能三:司机查询订单状态**
1. 业务逻辑
司机接单后，需要实时了解订单当前状态，以便进行相应操作


2. 流程
```text
司机端(轮询) → 查询订单状态 → 返回状态值 → 根据状态展示不同界面
```

3. web-driver(OrderController)
```java
    @Operation(summary = "查询订单状态")
    @GetMapping("/getOrderStatus/{orderId}")
    public Result<Integer> getOrderStatus(@PathVariable Long orderId) {
        return Result.ok(orderService.getOrderStatus(orderId));
    }
```
## **功能四:司机更新司机位置**
1. 背景知识
Redis GEO
- Redis的Geo主要用于存储地理位置信息，并对存储的信息进行操作
- Redis里面缓存的是司机的定位信息，用代驾单的起点坐标来查询附近几公里以内的司机
- 而且Redis的Geo计算是在内存中完成的，比MySQL的Geo计算快了上千倍。


2. 业务逻辑
- 司机开启接单服务后，司机端小程序就会实时上传经纬度信息到redis的GEO
- 关闭接单服务我们就要清空GEO数据
- 司机针对接单，有一些个性化设置，只有满足了这些条件，才可以接单
  - 比如是否开启接单, 接单里程设置, 是否自动抢单……

3. 流程
```text
司机端(定时) → 获取司机个性化设置消息 → 更新位置 → 检查服务状态 → 存入Redis GEO

司机端 → 关闭服务 → 删除GEO位置 → 更新服务状态
```

4. web-driver(LocationController)
```java
    @Operation(summary = "开启接单服务：更新司机经纬度位置")
    @PostMapping("/updateDriverLocation")
    public Result<Boolean> updateDriverLocation(@RequestBody UpdateDriverLocationForm updateDriverLocationForm) {
        Long driverId = Long.parseLong(SecurityContextHolder.getContext().getAuthentication().getName());
        updateDriverLocationForm.setDriverId(driverId);
        return Result.ok(locationService.updateDriverLocation(updateDriverLocationForm));
    }
```

## **功能五:搜索附近司机**
1. 业务逻辑
根据乘客起点位置，搜索附近符合条件且开启接单的司机

2. 流程
```text
获取起点经纬度 → Redis GEO搜索 → 获取司机设置 → 过滤 → 返回符合条件的司机列表
```

## **使用Swagger进行接口测试**

### 启动 service-map 和service-driver 
- 访问:http://localhost:8503/doc.html

1. 更新司机位置: /map/location/updateDriverLocation
2. 测试数据
```json
{
  "driverId": 1,
  "longitude": 116.417876,
  "latitude": 39.915411
}
```
```json
{
  "driverId": 2,
  "longitude": 116.517876,
  "latitude": 39.815411
}
```
3. 查看redis客户端,有新增数据
```bash
127.0.0.1:6379> keys *
1) "driver:geo:location"
```


1. 搜索附近满足条件的司机: /map/location/searchNearByDriver 
2. 测试数据
```json
{
  "longitude": 116.527876,
  "latitude": 39.825411,
  "mileageDistance": 15
}
```
3. 测试结果
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "driverId": 2,
      "distance": 1.4
    }
  ]
}
```

## **功能六:任务调度**
1. 业务逻辑
- 乘客下单了，然后启动一个任务调度，每隔1分钟执行一次搜索附近司机的任务调度
- 只要在15分钟内没有司机接单，那么就必须一直查找附近适合的司机，直到15分钟内有司机接单为止。
- 任务调度搜索到满足条件的司机后，会在服务器端给司机建立一个临时队列（1分钟过期），把新订单数据放入队列
- 司机小程序端开启接单服务后，每隔几秒轮询获取临时队列里面的新订单数据，在小程序前端进行语音播报
- 司机即可进行抢单操作。

本项目选择 XXL-JOB
- 是一个轻量级分布式任务调度框架
- 其核心设计目标是开发迅速、学习简单、轻量级、易扩展
- 官方文档: https://www.xuxueli.com/xxl-job/
- spring cloud项目集成xxl-job
  - 参考文章: https://cloud.tencent.com/developer/article/2413448
  - 从github上克隆项目(或者将资料包里的直接复制过来):  git clone https://github.com/xuxueli/xxl-job.git
  - 将 xxl-job 项目复制到当前项目中
  - 在父模块的pom.xml将xxl-job当成模块引入 
  - 执行xxl-job项目里的doc/db/tables_xxl_job.sql里的语句(查看xxl_job_user里的用户名和密码,用于登录)
  - 修改xxl-job里的xxl-job-admin模块的application.properties配置里的数据库信息
  - 启动xxl-job-admin模块
  - 访问网址: http://localhost:8080/xxl-job-admin
  - 用户名: admin
  - 密码: 123456
  - 在'任务管理'里新增任务后启动任务,
  - 在service-dispatch模块的pom.xml里引入xxl-job依赖,在父模块进行依赖版本管理
  - 启动xxl-job-admin模块
  - 启动service-dispatch模块
  - 测试


2. 流程

## **功能七:司机获取新订单列表**
1. 业务逻辑
   司机开启接单后，需要获取系统推送的新订单信息，进行语音播报和抢单


2. 流程
```text
司机端(轮询) → 获取新订单 → 从Redis队列取出 → 返回订单列表 → 前端播报

数据流转：
调度任务(每分钟) → 搜索附近司机 → 过滤设置条件 → 放入司机队列 → 司机轮询获取
```

## 实体类
- com.daijia.model.entity.dispatch.OrderJob
- com.daijia.model.entity.dispatch.XxlJobInfo
- com.daijia.model.entity.dispatch.XxlJobLog

```sql
CREATE DATABASE IF NOT EXISTS `daijia_dispatch`;
USE `daijia_dispatch`;

#
# Structure for table "order_job"
#

CREATE TABLE `order_job` (
  `id` bigint(11) NOT NULL AUTO_INCREMENT,
  `order_id` bigint(20) NOT NULL DEFAULT '0' COMMENT '订单id',
  `job_id` bigint(11) NOT NULL DEFAULT '0' COMMENT '任务id',
  `parameter` text COMMENT '参数',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` tinyint(3) NOT NULL DEFAULT '0' COMMENT '删除标记（0:不可用 1:可用）',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uni_order_id` (`order_id`),
  UNIQUE KEY `uni_job_id` (`job_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单与任务的关联表';


CREATE TABLE `order_status_log` (
                                  `id` bigint(11) NOT NULL AUTO_INCREMENT,
                                  `order_id` bigint(11) DEFAULT NULL,
                                  `order_status` varchar(11) DEFAULT NULL,
                                  `operate_time` datetime DEFAULT NULL,
                                  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
                                  `is_deleted` tinyint(3) NOT NULL DEFAULT '0',
                                  PRIMARY KEY (`id`),
                                  KEY `idx_order_id` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='订单状态日志记录表';

#
# Structure for table "xxl_job_log"
#

CREATE TABLE `xxl_job_log` (
                               `id` bigint(11) NOT NULL AUTO_INCREMENT,
                               `job_id` bigint(11) NOT NULL DEFAULT '0' COMMENT '任务id',
                               `status` int(11) NOT NULL DEFAULT '1' COMMENT '任务状态    0：失败    1：成功',
                               `error` text COMMENT '失败信息',
                               `times` int(11) NOT NULL DEFAULT '0' COMMENT '耗时(单位：毫秒)',
                               `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                               `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
                               `is_deleted` tinyint(3) NOT NULL DEFAULT '0' COMMENT '删除标记（0:不可用 1:可用）',
                               PRIMARY KEY (`id`),
                               KEY `idx_job_id` (`job_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

#
# Data for table "xxl_job_log"
#

```

---
## **错误1: service-map**

```text
Cannot deserialize value of type `java.util.Date` from String "2026-02-13T14:51:32.000+00:00"
```

**分析问题:**
Feign 在反序列化 DriverSet 对象时，无法将字符串 "2026-02-13T14:51:32.000+00:00" 转换成 java.util.Date 类型。
这是因为两边的日期格式配置不一致。

**解决办法:**
1. 在实体类时间字段上添加注解
```java
    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ss.SSSXXX")  // 添加这个注解
    private Date createTime;
```

2. 配置文件添加
```yaml
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
```


## **错误2: web-customer(OrderServiceImpl)**

```text
feign.FeignException$InternalServerError: [500] during [POST] to [http://service-order/order/info/saveOrderInfo] [OrderInfoFeignClient#saveOrderInfo(OrderInfoForm)]: [{"timestamp":"2026-02-18 17:00:13","status":500,"error":"Internal Server Error","path":"/order/info/saveOrderInfo"}]
```

Result<Long> orderInfoResult = orderInfoFeignClient.saveOrderInfo(orderInfoForm);
调用出错


## **错误3:**

启动xxl-job-admin模块,访问http://localhost:8080/xxl-job-admin,使用admin/123456登录失败

解决:
- 文章: https://blog.csdn.net/sniper_fandc/article/details/153269494
- 是因为不同版本的xxl-job使用不同的加密算法,而
- 修改数据库里的密码

```sql

-- 更新 admin 用户的密码和角色
UPDATE xxl_job_user 
SET password = 'e10adc3949ba59abbe56e057f20f883e',
    role = 1
WHERE username = 'admin';


-- 验证更新结果
SELECT * FROM xxl_job_user WHERE username = 'admin';

```

```bash
# 查看当前分支和修改
git status
# 创建分支
git checkout -b feature/passenger-place-order

git add .
git commit -m "feat:完成乘客下单功能"
git push -u origin feature/passenger-place-order
```