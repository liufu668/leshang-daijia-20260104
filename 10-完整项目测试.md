## **启动中间件:**
- redis
- nacos
- rabbitmq
- seata
- mongodb(不用手动启动)

```bash
# 打开cmd输入命令启动rabbitmq
rabbitmq-server start

```

批量删除调度任务
xxl_job 数据库batchGetDriverSet
```sql
-- 批量停止所有任务（trigger_status=0）
UPDATE xxl_job_info SET trigger_status = 0, update_time = NOW();

-- 验证停止结果（查询运行中任务数量，应为0）
SELECT COUNT(*) AS running_job_count FROM xxl_job_info WHERE trigger_status = 1;

```

**性能优化思路:**

- 基准压测,得到原始数据
- 监控排查
  - 应用层
  - 缓存层
  - 数据库
- 梯度压测(针对性压测)



## **启动模块:**
- xxl-job-admin
- ServerGatewayApplication
- ServiceCouponApplication
- ServiceCustomerApplication
- ServiceDispatchApplication
- ServiceDriverApplication
- ServiceMapApplication
- ServiceOrderApplication
- ServicePaymentApplication
- ServiceRulesApplication
- WebCustomerApplication
- WebDriverApplication


## 微信开发者工具减小占用内存
- "通用设置"取消勾选"GPU加速"
- 乘客端关闭"模拟器",启用真机调试
  - 真机调试微信登录时提示"网络连接故障",关闭电脑防火墙即可
- 司机端正常使用"模拟器"
- "通用设置"里内存限制从"1024"增大为"2048"
  - 否则运行过程中由于内存不足,会编译失败


## **从乘客下单到完成订单的整个流程**

### **简洁版**

- 乘客端微信登录
  - 在首页选择代驾的起点和终点
  - 预估订单里程和价格
  - 选择呼叫代驾(提交订单并搜索周围5公里以内符合条件的司机)
- 司机端微信登录
  - 开始接单前先进行个人身份认证(上传身份证和驾驶证),然后进行每日的人脸认证
  - 开始接单,上传个人位置信息
- 乘客端发现符合条件的司机,向他推送订单
- 司机端抢单
  - 抢单成功后,司乘同显
  - 司机到达乘客起始点
  - 录入车辆信息
  - 开始代驾服务
  - 结束代驾服务
  - 填写账单
  - 将账单推送到乘客端
- 乘客端选择优惠劵,确认支付账单,生成二维码,扫码支付账单
- 完成代驾服务


### **详细版**

- 乘客端微信登录
  - 当前没有未完成的订单(searchCustomerCurrentOrder),就可以开始新的订单
- 乘客在首页选择代驾的起点和终点
  - 预估订单数据(expectOrder): 计算订单里程(calculateDrivingLine)和费用(calculateOrderFee),
- 乘客点击"呼叫代驾"
  - 提交订单(submitOrder): 计算订单里程和价格,将订单数据保存到数据库表 OrderInfo 里(saveOrderInfo),同时触发司机搜索任务(addAndStartTask)
  - 保存好订单数据到数据库后,将订单消息放到延迟队列里,15分钟无人接单自动取消(sendDelayMessage)
  - 将订单接单标识加入到 redis 里
  - 调用调度中心创建并启动任务(addAndStart)
    - 项目启动阶段:
      - XxlJobConfig 被Spring加载，创建 XxlJobSpringExecutor Bean 
      - 当前应用作为执行器向调度中心注册（每30秒心跳） 
      - XxlJobClientConfig 读取配置，准备HTTP调用参数
    - 业务触发创建任务(addAndStart)
    - XXL-JOB客户端调用调度中心(addAndStart)
    - 调度中心触发任务执行 
      - 调度中心根据Cron表达式（每分钟）触发任务 
      - 通过RPC调用执行器（当前应用） 
      - 执行器找到对应的 @XxlJob 方法
    - 执行器执行业务代码(newOrderTaskHandler)
      - 搜索附近符合条件的代驾司机(executeTask)
  - 查询订单状态(getOrderStatus),看是否有司机接单
- 司机端微信登录
- 司机点击"开始接单"
  - 判断司机是否符合接单条件(startService)
    - 是否通过了司机认证(getDriverLoginInfo)
    - 是否进行了每日的人脸识别(isFaceRecognition)
    - 更新司机全局接单状态(updateServiceStatus)
  - 当前是否有未完成的订单(searchDriverCurrentOrder),没有则开始接单
  - 开始接单后,司机端实时上传司机位置的经纬度信息到redis的GEO(updateDriverLocation)
  - 任务调度从 Redis GEO 里找到符合接单条件的司机集合(searchNearByDriver)
  - 为每个符合接单条件的司机生成临时队列，把新订单数据放入队列，创建抢单标识,设置队列1分钟过期(executeTask)
  - 司机端实时轮询司机服务器端临时队列(findNewOrderQueueData),发现有合适的订单后,进行抢单
- 司机点击"抢单"(robNewOrder)
  - 创建 Redisson 分布式锁
  - 获取锁(tryLock)成功后,订单状态改为"已接单",删除 redis 里的订单抢单标识
  - 抢单成功,关闭接单服务(stopService),更改司机接单状态,删除 redis GEO 里的司机位置信息,清除司机临时队列
- 司机端赶往乘客起始点
  - 司乘同显(calculateDrivingLine),根据最佳线路在小程序地图组件上渲染
  - 实时更新司机的经纬度位置到Redis缓存(updateOrderLocationToCache)
- 乘客端实时显示司机的位置信息(getCacheOrderLocation),订单信息(getOrderInfo)和司机信息(getDriverInfo)
- 司机赶到代驾起点,点击“到达乘客起点”(driverArriveStartLocation)
- 司机点击"录入车辆信息"(updateOrderCart)
- 司机开始代驾服务(startDrive)
  - 代驾过程中,存储司机的GPS定位信息(saveOrderServiceLocation)到MongoDB数据库集合中(order_service_location),定时批量上传到后台服务器
  - 开始代驾时,初始化订单监控统计数据(saveOrderMonitor)
  - 代驾过程中,录制司乘对话,上传录音文件(upload),保存录音和审核结果(saveMonitorRecord)到MongoDB数据库集合里(order_monitor_record)
  - 更新MySQL数据库里的订单表数据(OrderMonitor)(updateOrderMonitor)
- 乘客端获取司机的动向
  - 定时从MongoDB数据库集合中(order_service_location)获取司机每次更新的最后一个位置信息(getOrderServiceLastLocation)
- 司机结束代驾服务(endDrive)
  - 输入其他费用（路桥费、停车费与其他费用）
  - 计算订单实际里程(calculateOrderRealDistance)
  - 计算代驾实际费用(calculateOrderFee),再加上 路桥费、停车费、其他费用、乘客好处费
  - 计算系统奖励(calculateOrderRewardFee)
  - 计算分账信息(calculateOrderProfitsharingFee)
  - 司机端跳转到账单确认页，展示账单信息，然后发送账单给乘客端(sendOrderBillInfo)
- 乘客端显示完整的订单信息
  - 拿到账单信息(getOrderBillInfo)
  - 查询未领取优惠券(findNoReceivePage)
  - 查询未使用优惠券(findNoUsePage)
  - 查询已使用优惠券(findUsedPage)
  - 领取优惠券(receive)
  - 默认选中优惠最大金额优惠券(findAvailableCoupon)
  - 使用优惠券(useCoupon)
  - 微信支付(createWxPayment)
  - 查询订单支付状态(queryPayStatus)
- 系统奖励添加到司机的个人账户(transfer)
- 乘客在"个人中心"点击"我的订单",进入订单详情页(findCustomerOrderPage)
- 司机在"个人中心"点击"我的订单",进入订单详情页(findDriverOrderPage)


# **使用JMeter进行压力测试**

- 通过命令行输入"jmeter"启动JMeter

## 1. 基准测试（摸清底细）
线程数：50
Ramp-Up：5秒
Duration：60秒
目标：看单线程能跑多少，建立基线

## 2. 负载测试（找最佳点）
线程数：100 → 200 → 300 → 500（逐步增加）
Ramp-Up：10秒
Duration：120秒（跑久一点，数据更稳定）
目标：找到QPS拐点（响应时间开始急剧上升的地方）

## 3. 压力测试（找极限）
线程数：800 → 1000
Ramp-Up：20秒
Duration：180秒
目标：看系统崩溃前的最大QPS

## 4. 稳定性测试（跑通宵）
线程数：200（最佳线程数）
Ramp-Up：10秒
Duration：3600秒（1小时）
目标：检查内存泄漏、连接池耗尽[citation:7]

## **测试模块一: 高并发抢单**

优化要点:
- 减少锁的粒度(全局一把锁——>订单锁)
- 并发安全
  - Redis 的 hasKey() 方法是非原子操作,改用 Lua 脚本
  - db 默认使用悲观锁(行锁),改用 version 字段做乐观锁
  - 保证释放的是线程自己持有的锁

- 100个司机抢一个订单,模拟并发安全
- 200个司机抢50个订单,测试性能

- 流程:
  - 乘客端发送下单请求(submitOrder 接口)并携带 Token, 同时开启任务调度, 在 Redis GEO 里搜寻司机
  - 司机端开始接单(updateDriverLocation 接口), 上传位置信息到 Redis GEO
  - 被任务调度发现, 推送订单到司机端的临时队列
  - 司机进行抢单(robNewOrder 接口)
- 测试数据准备:
  - 准备一个订单数据,确认订单的经纬度位置(SubmitOrderForm)
  - 准备100个司机数据,位置在订单位置周围5公里以内(UpdateDriverLocationForm)
  - 插入100个司机数据到 driver_set数据库表中
- 三个 Thread Group ,三个 HTTP Request
  - 乘客端下单, submitOrder 接口(传递 SubmitOrderForm, customerId)
  - 司机端开始接单, updateDriverLocation 接口(传递 UpdateDriverLocationForm, driverId)
  - 司机端抢单, robNewOrder 接口(传递 orderId, driverId)
- 绕过拦截登录认证: 注销掉拦截器 TokenAuthenticationFilter 的 @Component; 在 WebSecurityConfig 放行请求
- 启动中间件和后端服务
  - xxl-job-admin
  - ServerGatewayApplication
  - ServiceCustomerApplication
  - ServiceDispatchApplication
  - ServiceDriverApplication
  - ServiceMapApplication
  - ServiceOrderApplication
  - ServiceRulesApplication
  - WebCustomerApplication
  - WebDriverApplication

```sql
-- 插入两百条数据到 driver_set 表中,司机ID从1001-1200
INSERT INTO driver_set (driver_id, service_status, order_distance, accept_distance, is_auto_accept, create_time, update_time, is_deleted)
WITH RECURSIVE numbers AS (
    SELECT 1001 AS n
    UNION ALL
    SELECT n + 1 FROM numbers WHERE n <= 1200
)
SELECT 
    n AS driver_id,
    1 AS service_status,
    0.00 AS order_distance,
    50.00 AS accept_distance,
    0 AS is_auto_accept,
    NOW() AS create_time,
    NOW() AS update_time,
    0 AS is_deleted
FROM numbers n
LEFT JOIN driver_set ds ON ds.driver_id = n.n
WHERE ds.driver_id IS NULL;
```


```sql

-- 在 order_info 表里添加 version 字段
ALTER TABLE order_info ADD COLUMN `version` INT DEFAULT 0 COMMENT '乐观锁版本号';

-- 删除 version 字段
ALTER TABLE order_info DROP COLUMN version;
```

## **测试模块二: 附近司机匹配接口**

- 预估订单时将订单里程和费用数据添加到缓存中(设置过期时间15分钟,过期后才下单则实时重新计算(缓存过期自动降级)),乘客下单时就不用重复计算
- 异步开启任务调度(异步 + 订单状态 + 定时补偿)
- 用多线程 CompletableFuture 实现串行任务变并行


- 启动 updateDriverLocation 接口,上传司机位置到Redis GEO
- 压测 LocationController 里的 searchNearByDriver 接口,主要是测查询司机的性能
- 优化 searchNearByDriver 接口的要点:
  - 

在 OrderInfo 表里添加 dispatchStatus 字段 
```sql
ALTER TABLE order_info ADD COLUMN `dispatch_status` TINYINT DEFAULT 0 COMMENT '订单调度状态';

ALTER TABLE order_info DROP COLUMN dispatch_status;

```


## **测试模块三: 乘客下单链路()**


- 预估订单时将订单里程和费用数据添加到缓存中(设置过期时间15分钟,过期后才下单则实时重新计算(缓存过期自动降级)),乘客下单时就不用重复计算
- 异步开启任务调度(异步 + 订单状态 + 定时补偿)
- 用多线程 CompletableFuture 实现串行任务变并行


性能提升的主要优化点:(调大参数)
```yaml
spring:
  task:
    execution:
      thread-name-prefix: order-async-
      core-size: 30
      max-size: 100
      keep-alive: 60s
      queue-capacity: 500

    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 1800000
      password:
      jedis:
        pool:
          max-active: 50  # 从 20 改成 50，和线程池核心数匹配
          max-wait: -1    #最大阻塞等待时间(负数表示没限制)
          max-idle: 20    # 从 5 改成 20，保留更多空闲连接
          min-idle: 5     # 从 0 改成 5，避免频繁创建销毁连接
```


```bash
# 查看当前分支和修改
git status
# 创建分支
git checkout -b feature/optimization

git add .
git commit -m "feat:优化项目（提高乘客下单后搜索附近司机的性能）"
git push -u origin feature/optimization

# 切换到master分支
git checkout master

# 合并到master
git merge feature/optimization

# 推送到master
git push origin master

```

