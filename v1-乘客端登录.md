# 乘客端登录系统需求文档

## 1. 概述
### 1.1 业务目标
完成乘客端微信授权登录

### 1.2 技术目标
- 掌握登录校验AOP拦截
- 处理当前用户信息

## 2. 技术栈
Spring Boot3.5.7:基础框架
MySQL:存储用户信息
Spring Security:用于认证授权流程控制
Redis:存储用户认证的token

### 3. 核心业务流程

- 用户点击"微信登录"
- 前端重定向到微信授权页
- 用户确认授权
### 3.1 登录流程（使用Redis）
- 前端：wx.login() 获取 code
- 前端：将 code 发送到后端
- 后端：
  用 appId + appSecret + code 向微信服务器换取 openid + session_key
  检查用户是否存在，不存在则创建
  生成一个随机Token
  将用户信息和session_key存入Redis，Key为Token，设置合理过期时间
- 前端：保存后端返回的Token
### 3.2 请求验证流程
- 前端：请求头携带 token
- 后端：
  从请求头获取Token
  拦截器验证Token有效性（查Redis是否存在）
  有效：从Redis获取用户信息，放入SecurityContext
  无效：返回401，跳转登录页
  执行后续业务逻辑

## 架构图：
业务流程图：
数据库ER图：

## 4. 接口设计

### 4.1 完成乘客端微信登录
1. 实现接口：GET /customer/login/{code}
2. 任务要求：
  调用微信API通过code换取openid
  根据openid查询乘客表，不存在则自动注册
  生成UUID token，存入Redis
  记录登录日志到 customer_login_log 表
  返回token给前端
3. 参数:
- code: string (微信返回的授权码)
4. 响应:
```json
  {
    "code": 200,
    "message": "success",
    "data": "xxx..."
  }
```
5. 测试结果:
乘客登录成功
前端接收到后端返回的token
前端回调"获取登录用户信息"接口,获取当前乘客基本信息

### 4.2 获取登录用户信息
1. 实现接口：GET /customer/getCustomerLoginInfo
2. 任务要求：
  从请求头获取token
  从Redis查询用户ID
  查询乘客信息并返回
3. 响应:
```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "id": "xx",
      "wxOpenId": "xx",
      "nickname": "",
      "gender": 1,
      "avatarUrl": "",
      "phone": "",
      "status": 1,
      "createTime": "xx",
      "updateTime": "xx",
      "isDeleted": 0
    }
  }
```
4. 测试结果
前端成功获取用户信息

### 4.3 更新用户信息
1. 实现接口: POST /customer/updateCustomerInfo
2. 参数:
```json
{
  "nickname": "用户昵称",
  "avatarUrl": "头像URL"
}
```
3. 流程:
后端：返回用户信息（新用户可能只有 openid，没有昵称和头像） 
前端：检查用户是否有昵称和头像
↓
如果没有 → 调用微信 getUserInfo 获取昵称和头像
↓
前端：调用 /customer/updateCustomerInfo 更新用户信息
↓
后端更新后返回完整的用户信息。
前端重新获取用户信息并存储
4. 响应:
```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "id": "xx",
      "wxOpenId": "xx",
      "nickname": "xx",
      "gender": 1,
      "avatarUrl": "xx",
      "phone": "",
      "status": 1,
      "createTime": "xx",
      "updateTime": "xx",
      "isDeleted": 0
    }
  }
```
5. 测试结果:
前端获取到的用户信息中包括nickname和avatarUrl

### 4.4 绑定微信手机号
1. 实现接口：POST /customer/updateWxPhone
2. 任务要求：
注意：个人微信小程序无法获取真实手机号，可做模拟处理
生成随机手机号："138" + 随机8位数字
直接更新数据库并返回手机号给前端
3. 响应:
```json
  {
    "code": 200,
    "message": "success",
    "data": "138****8888"
  }
```
4. 参数：
code（微信手机号授权码）
5. 测试结果:
前端和后端的用户信息都有生成的手机号信息

## 5. 业务规则

1. 只有登录,没有注册,微信授权登录
2. 自动创建账号，昵称和头像使用微信的
3. 所有登录尝试记录记录到 customer_login_log 表
4. 每个code只能使用一次,有效期5min

## 6. 数据表格

```sql
#
# Database "daijia_customer"
#

CREATE DATABASE IF NOT EXISTS `daijia_customer`;
USE `daijia_customer`;

CREATE TABLE `customer_info` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '主键',
  `wx_open_id` varchar(200) NOT NULL DEFAULT '' COMMENT '微信openId',
  `nickname` varchar(200) DEFAULT '' COMMENT '客户昵称',
  `gender` char(1) NOT NULL DEFAULT '1' COMMENT '性别',
  `avatar_url` varchar(200) DEFAULT NULL COMMENT '头像',
  `phone` char(11) DEFAULT NULL COMMENT '电话',
  `status` tinyint(3) DEFAULT '1' COMMENT '1有效，2禁用',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint(3) NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`) USING BTREE,
  UNIQUE KEY `uni_open_id` (`wx_open_id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8 ROW_FORMAT=DYNAMIC COMMENT='客户表';

CREATE TABLE `customer_login_log` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '访问ID',
  `customer_id` varchar(50) DEFAULT '' COMMENT '客户id',
  `ipaddr` varchar(128) DEFAULT '' COMMENT '登录IP地址',
  `status` tinyint(1) DEFAULT '1' COMMENT '登录状态',
  `msg` varchar(255) DEFAULT '' COMMENT '提示信息',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `update_time` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint(3) NOT NULL DEFAULT '0' COMMENT '删除标记（0:不可用 1:可用）',
  PRIMARY KEY (`id`),
  KEY `idx_customer_id` (`customer_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='客户登录记录';
```

## 7. 提交代码到GitHub
1. 创建分支
   main分支 → 永远保持稳定、可部署状态
   develop分支 → 开发集成测试
   feature分支 → 开发新功能、实验性代码
2. 清晰版本管理
   每个功能在独立分支开发
   完成后合并到主分支
   便于代码审查和回滚
```bash
# 初始化仓库
git init

# 确保本地有master分支
git branch
# 如果显示 * master，继续下一步
# 如果没有master分支：git checkout -b master

# 如果是空仓库，需要先添加一个初始文件并提交
git add v1-乘客端登录.md
git commit -m "初始提交：项目框架"

# 查看已配置的远程仓库
git remote -v
# 如果没有任何输出，说明确实没有设置 origin

# 添加远程仓库地址
git remote add origin https://github.com/liufu668/leshang-daijia-20260104.git

# 验证是否添加成功
git remote -v
# 应该显示：
# origin  https://github.com/liufu668/leshang-daijia-20260104.git (fetch)
# origin  https://github.com/liufu668/leshang-daijia-20260104.git (push)

# 从main创建develop分支
git checkout -b develop

# 推送到GitHub
git push -u origin develop

# 查看分支情况
git branch -a
# 应该看到：
# * develop
#   master
#   remotes/origin/develop
#   remotes/origin/master

# 从develop创建功能分支（回到develop分支）
git checkout develop

# 创建你的登录功能分支
git checkout -b feature/passenger-login-v1

# 查看当前分支
git branch
# 应该显示：* feature/passenger-login-v1

# 现在你在 feature/passenger-login-v1 分支
git add .
git commit -m "feat: 乘客登录系统基础版本 v1.0

# 第一次推送需要 -u 参数
git push -u origin feature/passenger-login-v1

# 后续开发只需
git add .
git commit -m "feat: 添加xx功能"
git push

# 查看远程分支
git branch -a
# 应该看到你的功能分支在远程

# 切换到develop分支
git checkout develop

# 合并功能分支
git merge feature/passenger-login-v1

# 提交合并
git commit -m "merge: 合并feature/passenger-login-v1到develop分支"

# 推送到远程develop
git push origin develop
# 切换到master分支
git checkout master

# 合并develop到master
git merge develop

# 推送到master
git push origin master

# 打上版本标签
git tag v1.0.0
git push origin --tags
```

