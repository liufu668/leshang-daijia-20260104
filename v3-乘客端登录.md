# 乘客端登录优化第三版(根据官方乐尚代驾项目代码进行优化)

## 1. 统一异常处理机制
- 全局统一返回结果类 Result<T>
- 抛弃JPA,改用 Mybatis-Plus,新建 CustomerInfoMapper 和 CustomerLoginLogMapper 两个接口
- 使用微信Java开发工具包简化获取 wxOpenId 的过程,导入 weixin-java-miniapp 依赖
- 把 CustomerController 里的登录核心代码移到 CustomerInfoServiceImpl
- 自定义全局异常管理类 GuiguException
- 


## 2. 搭建微服务架构

### 2.1 微服务架构
**微服务架构是一种将单一应用程序拆分为一组小型、独立的服务的架构风格，每个服务：**
- 独立运行在自己的进程中 
- 独立部署和扩展 
- 独立开发和维护 
- 通过轻量级通信机制（通常是HTTP/REST或消息队列）相互通信 
- 围绕业务能力而非技术能力进行组织

**完整的微服务生态系统:**
- 用户体验层(App/Web/小程序)
  - API网关层(路由转发; 认证授权; 限流熔断)
    - 服务治理层(服务注册发现(Nacos); 配置中心(Nacos); API管理(Swagger))
      - 业务服务层(用户服务/订单服务/支付服务/商品服务,每个服务都是独立部署,独立数据库)
        - 数据基础设施层(缓存(Redis); 消息队列(RabbitMQ); 搜索引擎(ES))
        - 监控运维层(链路追踪(SkyWalking); 日志收集(ELK); 指标监控(Prometheus))
          - 容器平台层(Kubernetes)
  - CDN/边缘计算   
    - 静态资源
    - 缓存加速
  - 消息推送
    - 实时通知 
    - WebSocket

#### 2.1.1 单体架构和微服务架构对比:
**单体架构:**
- 单一应用程序(共享数据库)
  - 用户模块
  - 订单模块
  - 商品模块
  - 支付模块

**微服务架构:**
- API网关/服务网格
  - 用户服务
    - 独立数据库
  - 订单服务
    - 独立数据库
  - 商品服务
    - 独立数据库
  - 支付服务
    - 独立数据库

API网关/服务网格

#### 2.1.2 微服务架构基础设施组件:
- API网关(客户端访问入口)
  - 服务注册发现; 配置中心; 监控  
    - 认证授权; 日志收集; 链路追踪
      - 容器编排（Kubernetes）       
        - 容器运行时（Docker）

#### 2.1.3 微服务核心组件:

**服务注册与发现**
- 服务启动时向注册中心注册,服务调用时从注册中心获取,服务由注册中心统一管理维护 

**API网关**
- 路由转发:
  - /user/**    → user-service
  - /order/**   → order-service
  - /payment/** → payment-service
- 安全控制:
  - 身份认证(JWT校验)
  - 权限验证(角色权限)
  - IP黑白名单
  - 防刷限流 
- 业务增强:
  - 请求/响应日志
  - 数据脱敏
  - 参数校验
  - API聚合(一个请求查多个服务)
- 容错处理:
  - 熔断降级(服务不可用时返回默认值)
  - 超时控制
  - 重试机制

**配置中心**
- 统一管理不同服务的配置,避免配置不一致
- 不重启服务,热更新

#### 2.1.4 微服务通信方式
- 同步通信
  - HTTP/REST（Feign、RestTemplate）
  - gRPC（高性能RPC）
  - RMI（Java远程调用）
- 异步通信
  - 消息队列
    - 发布-订阅（RabbitMQ）
    - 流处理（Kafka）
  - 事件驱动
    - 事件溯源
    - CQRS

**同步通信:**
```java
// 场景：下单时需要立即验证用户信息
@PostMapping("/orders")
public Order createOrder(@RequestBody OrderRequest request) {
    // 1. 同步调用用户服务（必须立即得到结果）
    User user = userServiceClient.getUser(request.getUserId());
    
    // 2. 同步调用库存服务
    boolean hasStock = stockServiceClient.checkStock(request.getProductId());
    
    // 3. 创建订单
    return orderService.createOrder(request);
}

// 特点：
// - 实时性强
// - 代码简单直观
// - 问题：服务链路过长会累积延迟
// - 适用：需要立即响应的操作
```

**异步通信(消息队列):**
```java
// 场景：下单成功后发送通知（不需要立即完成）
@Service
public class OrderService {
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    public Order createOrder(OrderRequest request) {
        // 1. 创建订单（核心业务）
        Order order = saveOrder(request);
        
        // 2. 发送消息到MQ（异步处理）
        rabbitTemplate.convertAndSend("order.exchange", 
                                     "order.created", 
                                     order.getId());
        // 立即返回，不等待通知发送完成
        return order;
    }
}

// 消费者（通知服务）
@Component
public class NotificationConsumer {
    @RabbitListener(queues = "order.queue")
    public void handleOrderCreated(String orderId) {
        // 发送短信、推送、邮件等
        notificationService.sendSMS(orderId);
        notificationService.sendEmail(orderId);
        // 这里处理慢不影响下单流程
    }
}

// 特点：
// - 解耦，提高响应速度
// - 削峰填谷（流量高峰时缓冲）
// - 提高系统可用性
// - 适用：非核心、耗时、可延后的操作
```
#### 2.1.5 微服务的数据管理
**数据库设计原则:每个服务有独立的数据库**
错误做法：共享数据库
所有服务访问同一个数据库：
user_service    → 读取 users表
order_service   → 读写 orders表, users表
payment_service → 读写 payments表, orders表

问题：
1. 表结构变更影响所有服务
2. 一个慢查询拖垮整个系统
3. 难以按服务拆分扩容

正确做法：数据库隔离
- 用户服务(独立user_db)：users; profiles; addresses
- 订单服务(独立order_db)：orders; items
- 支付服务(独立payment_db)：payments; accounts

**数据一致性解决方案:**
```java
// 场景：下单扣库存（需要保证一致性）

// 方案1：分布式事务（Seata）
@GlobalTransactional  // ← 开启分布式事务
public Order createOrder(OrderRequest request) {
    // 1. 扣减库存
    stockService.reduceStock(request);
    // 2. 创建订单
    Order order = orderService.create(request);
    // 3. 扣减余额
    accountService.deductBalance(request);
    
    // 任何一个失败，全部回滚
    return order;
}

// 方案2：最终一致性（消息队列 + 事务消息）
public Order createOrder(OrderRequest request) {
    // 1. 预扣库存（先占位）
    stockService.preReduceStock(request);
    
    // 2. 发送事务消息（RocketMQ支持）
    TransactionMQProducer.sendMessageInTransaction(
        "order_topic", 
        orderCreatedEvent,
        // 本地事务执行
        () -> {
            // 3. 创建订单
            Order order = orderService.create(request);
            return order.getId();
        }
    );
    
    // 4. 异步执行：
    // - 实际扣库存
    // - 扣余额
    // - 发通知
}
```

### 2.2 微服务分层架构
**在微服务架构中，分层设计是保证系统可维护性和可扩展性的关键。**

### 架构图:
- ┌─────────────────┐
- │   小程序/客户端   │
- └────────┬────────┘
- │ HTTP/HTTPS
- ┌────────▼────────┐    
- │Web层(Controller) ◄———──┤   API网关     
- │(web-customer)       
- └────────┬────────┘
- │ Feign调用
- ┌────────▼────────┐
- │Service层(Service)│
- │(service-customer)│
- └────────┬────────┘
- │
- ┌────────▼────────┐
- │    数据库/Redis  │
- └─────────────────┘


## 3. 将项目的单体架构改成微服务架构

### 3.1 步骤
- 新建子模块
- 将父模块的代码文件分散到子模块里
- 整理pom.xml文件
- 整理代码内容
  - 添加服务注册与发现(Nacos)
    - 添加依赖
    - 添加配置文件 bootstrap.properties
    - 添加启动类
    - 将配置文件整理到一块,上传到nacos,项目从nacos获取配置信息
    - 下载Nacos,访问Nacos控制台
    - 启动服务后,Nacos控制台有对应的服务
  - 改造服务间调用,创建 Feign 客户端接口
  - 统一认证和跨服务用户信息传递
  - 独立的数据库和配置文件处理
- 运行项目

### 3.2 项目结构:
- common
  - common-util
    - .../result
      - Result
      - ResultCodeEnum
  - service-security
    - .../config
      - JwtProperties
      - WebSecurityConfig
  - service-util
    - .../config/redis
      - RedisConfig
    - .../exception
      - GuiguException
- model
  - .../entity/customer
    - CustomerInfo
    - CustomerLoginLog
  - .../vo/customer
    - CustomerInfoVo
    - CustomerLoginVo
    - UpdateWxPhoneVo
- server-gateway
  - .../gateway
- service
  - service-customer
    - .../config
      - WeChatProperties
      - WxMaConfig
      - TokenAuthenticationFilter
      - JwtTokenProvider
      - TokenService
    - .../controller
      - CustomerController
    - .../mapper
      - CustomerInfoMapper
      - CustomerLoginLogMapper
    - .../service
      - CustomerInfoService
    - .../service/impl
      - CustomerInfoServiceImpl
- service-client
  - service-customer-client
    - .../client
      - CustomerInfoFeignClient
- web
  - web-customer
    - .../controller
      - CustomerController
    - .../service
      - CustomerService
    - .../service/impl
      - CustomerServiceImpl
- DEFAULT_GROUP
  - common-account.yaml: 微信配置
  - server-gateway-dev.yaml: 网关配置
  - service-customer-dev.yaml: 服务配置
  - web-customer-dev.yaml: Web层配置


### 3.3 检查顺序
- 检查配置文件
- 入口检查
  - 检查网关和安全配置
- 认证流程检查
- 数据流检查(从外到内)
  - Web层,前端直接调用
  - Feign客户端
  - 服务层,微服务内部
  - 数据访问层
- 检查实体类和工具类
- 核心功能检查
  - 用户登录
  - 获取用户信息
  - 更新手机号

### 3.4 测试
- 启动nacos,访问nacos网页端:http://192.168.101.12:8848/nacos
- 在nacos网页端的"配置列表"手动添加配置文件
- 启动redis,mysql
- 按依赖顺序启动子模块
  - server-gateway
  - web-customer
  - service-customer
- 查看nacos网站的"服务列表", 看服务是否注册成功
- 启动微信小程序, 测试功能
