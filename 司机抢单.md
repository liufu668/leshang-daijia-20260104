# 司机抢单

## **功能一:开启接单**

1. 流程:
- 判断司机认证状态，只要认证通过了的司机才能接单，司机登录信息包含司机认证状态（已开发）
- 判断司机当日是否已做人脸识别，司机每天必须做一次人脸识别，如果未做，跳转到人脸识别页面
- 更新司机全局接单状态
- 删除司机之前的geo数据（重新做初始化，防止数据影响）
- 清空司机新订单临时队列（重新做初始化，防止数据影响）

## **功能一:司机抢单**

1. 流程:
- 当前司机已经开启接单服务了，实时轮流司机服务器端临时队列
- 只要有合适的新订单产生，那么就会轮回获取新订单数据，进行语音播放
- 如果司机对这个订单感兴趣就可以抢单
- 一个新订单会放入满足条件的所有司机的临时队列，谁先抢到就是谁的。

因为Redis具备高性能、高可用、高并发的特性，所以使用Redis实现分布式锁


## **注意事项**
在进行司机信息认证时
1. 创建人脸识别模型之后,腾讯云返回模型id,更新到数据库表,driver_info的face_model_id字段
    人脸识别模型只会创建一次,如果不慎删除了数据库里的id,可以到官网的人员库里删掉数据后重新进行认证
   https://console.cloud.tencent.com/aiface/face-lib/ZGFpamlhLWRyaXZlcg%3D%3D/person-list
2. 提交认证资料后,需要后台管理员人工认证审核,修改auth_status字段的值,通过认证则设为2(当前项目需要手动修改字段值)
```sql
UPDATE `daijia_driver`.`driver_info` SET `auth_status` = '2' WHERE (`id` = '2');
```


```bash
# 查看当前分支和修改
git status
# 创建分支
git checkout -b feature/driver-rob-order

git add .
git commit -m "feat:完成司机抢单功能"
git push -u origin feature/driver-rob-order
```