wechat:
  app-id: ${WECHAT_APPID}
  app-secret: ${WECHAT_APP_SECRET}

jwt:
  secret: ${JWT_SECRET}
  expiration-hours: 24

tencent:
  cloud:
    secretId: ${TENCENT_SECRETID}
    secretKey: ${TENCENT_SECRETKEY}
    region: ap-nanjing
    bucketPrivate: ${TENCENT_BUCKET}
    personGroupId: daijia-driver
